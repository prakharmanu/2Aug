    def toLowerCase(self, str: str) -> str:
        s=''
        for i in str:
            if i >= "A" and i<= "Z":
                s += chr(ord(i)+32)
            else:
                s += i
        return (s)
