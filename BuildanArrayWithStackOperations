    def buildArray(self, target: List[int], n: int):
        ans = []
        for i in range(1,target[-1]+1):
            if i in target:
                ans.append("Push")
            else:
                ans.append("Push")
                ans.append("Pop")
        return ans
